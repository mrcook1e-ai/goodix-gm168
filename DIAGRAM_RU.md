# Как работает драйвер отпечатка Goodix GM168 — простым языком

> Схема для тех, кто хочет понять что происходит, когда ты прикладываешь палец к сенсору на ноутбуке.

---

## 🗺️ Общая картина

```mermaid
flowchart TD
    FINGER["👆 Ты прикладываешь палец"]
    SENSOR["📡 Сенсор GM168SEC\n(чип в ноутбуке)"]
    USB["🔌 USB кабель\n(данные едут по проводу)"]
    DRIVER["🐧 Наш драйвер\n(программа на Linux)"]
    LINUX["🔐 Linux решает:\nпустить тебя или нет?"]

    FINGER --> SENSOR
    SENSOR -->|"зашифрованные данные"| USB
    USB --> DRIVER
    DRIVER -->|"обработанное изображение"| LINUX

    style FINGER fill:#d3f9d8,stroke:#2f9e44
    style SENSOR fill:#d0ebff,stroke:#1971c2
    style USB fill:#fff3bf,stroke:#e67700
    style DRIVER fill:#ffe3e3,stroke:#e03131
    style LINUX fill:#d3f9d8,stroke:#2f9e44
```

**Коротко:** сенсор снимает отпечаток → шифрует → отправляет по USB → драйвер расшифровывает и обрабатывает → Linux сравнивает и пускает тебя.

---

## 🔑 Шаг 0: Секретный ключ (делается один раз)

```mermaid
flowchart LR
    subgraph WIN["💻 Windows (один раз при настройке)"]
        W1["Goodix читает\nзашифрованный ключ\nиз памяти сенсора"]
        W2["Windows расшифровывает\nчерез DPAPI\n(системный сейф Windows)"]
        W3["Получаем psk.bin\n32 байта — секрет"]
        W1 --> W2 --> W3
    end

    subgraph LINUX["🐧 Linux (каждый раз)"]
        L1["/etc/goodix-gm168/psk.bin\nлежит на диске"]
        L2["Драйвер читает ключ\nпри старте"]
        L1 --> L2
    end

    W3 -->|"копируешь файл\nвручную"| L1

    style WIN fill:#dbe4ff,stroke:#4c6ef5
    style LINUX fill:#d3f9d8,stroke:#2f9e44
```

> ⚠️ **Почему так сложно?** Ключ намертво привязан к Windows-установке через DPAPI (это как сейф, открыть который может только та самая Windows). Поэтому надо один раз загрузиться в Windows, вытащить ключ и скопировать на Linux.

---

## 🚀 Шаг 1: Инициализация (драйвер запускается)

```mermaid
flowchart TD
    START["🟢 Драйвер запускается\n(ты залогинился или разблокировал экран)"]

    S1["📨 Wakeup\nпробуждаем сенсор (0x11)\n«Эй, ты тут?»"]
    S2["🔄 Reset\nсбрасываем в начальное состояние (0x60)\n«Забудь всё предыдущее»"]
    S3["📋 Version\nспрашиваем версию прошивки (0x20)\n«Кто ты такой?»"]
    S4["🔑 Загрузка ключа\nчитаем psk.bin с диска\n«Достаём секрет из сейфа»"]
    S5["🤝 TLS Handshake\nустанавливаем зашифрованный канал\nкак HTTPS но по USB\n(туда-сюда несколько пакетов)"]
    S6["🔒 Сессия установлена\nтеперь всё зашифровано\n«Туннель готов»"]
    S7["⚙️ Настройка сенсора\nArm (0xAE) + FDT (0x32)\n«Начни следить за пальцем»"]
    S8["🌑 Тёмные кадры ×5\nснимаем 5 кадров БЕЗ пальца\nусредняем → фон для вычитания\n«Калибровка нуля»"]
    S9["✅ Готов к работе\nждём пальца"]

    START --> S1 --> S2 --> S3 --> S4 --> S5 --> S6 --> S7 --> S8 --> S9

    style START fill:#d3f9d8,stroke:#2f9e44
    style S4 fill:#fff3bf,stroke:#e67700
    style S5 fill:#f3d9fa,stroke:#9c36b5
    style S6 fill:#f3d9fa,stroke:#9c36b5
    style S8 fill:#e7f5ff,stroke:#1971c2
    style S9 fill:#d3f9d8,stroke:#2f9e44
```

> 💡 **Тёмные кадры** — это как фотоаппарат, который сначала снимает с закрытым объективом, чтобы знать какой "шум" есть у матрицы. Потом это вычитается из реального снимка.

---

## 👁️ Шаг 2: Ожидание пальца

```mermaid
flowchart TD
    WAIT["⏳ Непрерывный цикл ожидания\nдрайвер слушает USB EP_IN 0x83"]

    P1["Пришёл пакет A0\necho=0x32, status=0x01\n«Пальца нет» → ждём дальше"]
    P2["Пришёл пакет A0\necho=0x32, status=0x02\n«ПАЛЕЦ ОБНАРУЖЕН!»"]

    WAIT --> P1
    P1 -->|"снова"| WAIT
    WAIT --> P2

    P2 --> CAPTURE["🚀 Запускаем захват кадра"]

    style WAIT fill:#e7f5ff,stroke:#1971c2
    style P1 fill:#f8f9fa,stroke:#aaa
    style P2 fill:#d3f9d8,stroke:#2f9e44
    style CAPTURE fill:#ffe3e3,stroke:#e03131
```

> 💡 Сенсор сам следит за нагрузкой (ёмкостный датчик). Когда палец касается — электрическое поле меняется → сенсор кричит «ЕСТь!»

---

## 📸 Шаг 3: Захват изображения

```mermaid
flowchart TD
    TRIG["📤 CAP_TRIG\nОтправляем команду 0x20\n«Сделай снимок!»\n+ 10 байт настроек"]

    RX["📥 CAP_RX\nЧитаем ответ по USB\nТаймаут = мин(остаток_бюджета, 1 сек)\nПакеты складываем в буфер"]

    PROC["⚙️ CAP_PROCESS\nРазбираем пакеты:\n• B0 → отдаём в TLS для расшифровки\n• B2 → тоже расшифровываем\n• A0 → пропускаем (подтверждения)\nСкладываем расшифрованное в img_buf"]

    CHECK{"Накопили ≥ 10560 байт?\n(это один полный кадр)"}

    MORE["Читаем ещё пакеты"]
    DONE["✅ Кадр получен!\nПередаём на обработку"]

    TRIG --> RX --> PROC --> CHECK
    CHECK -->|"нет"| MORE --> RX
    CHECK -->|"да"| DONE

    style TRIG fill:#ffe3e3,stroke:#e03131
    style RX fill:#ffe3e3,stroke:#e03131
    style PROC fill:#ffe3e3,stroke:#e03131
    style DONE fill:#d3f9d8,stroke:#2f9e44
```

> 💡 **Почему 10560 байт?** Сенсор 80×132 пикселя × 1 байт = 10560. Данные приходят кусками по USB, поэтому мы их склеиваем пока не получим полный кадр.

---

## 🔐 Как работает шифрование

```mermaid
flowchart LR
    subgraph SENSOR["📡 Сенсор"]
        RAW["Сырое изображение\n10560 байт"]
        ENC["Шифруем\nAES-128-CBC"]
        RAW --> ENC
    end

    subgraph WIRE["🔌 USB провод"]
        PKT["B2 пакеты\n(зашифровано)"]
        ENC --> PKT
    end

    subgraph DRIVER["🐧 Драйвер"]
        TLS["TLS слой\ngoodix_tls.c\npthread + socketpair"]
        PSK["psk.bin ключ\n32 байта"]
        PLAIN["Расшифрованные данные\n10560 байт — готово!"]
        PKT --> TLS
        PSK --> TLS
        TLS --> PLAIN
    end

    style SENSOR fill:#d0ebff,stroke:#1971c2
    style WIRE fill:#fff3bf,stroke:#e67700
    style DRIVER fill:#ffe3e3,stroke:#e03131
```

> 💡 **TLS** — это тот же протокол что защищает HTTPS в браузере. Только здесь он работает по USB, а не по сети. **PSK** (Pre-Shared Key) — общий секрет, который и сенсор и драйвер знают заранее.

---

## 🖼️ Шаг 4: Обработка изображения (7 этапов)

```mermaid
flowchart TD
    RAW["📊 Сырые данные\n10560 байт\n80 строк × 132 байт"]

    D1["1️⃣ decode_frame()\nРаспаковка 12-бит → 16-бит\nУбираем служебные байты (36 на строку)\nТранспонируем → 64×80 пикселей\nРезультат: raw16[5120]"]

    D2["2️⃣ bg_subtract()\nВычитаем тёмный фон (снятый при старте)\nПотом median 3×3 — убираем одиночные\nбитые пиксели (мусорные точки)"]

    D3["3️⃣ binomial3x3()\nГауссово размытие 3×3\nМатрица весов: 1 2 1 / 2 4 2 / 1 2 1 ÷ 16\nСглаживаем мелкий шум"]

    D4["4️⃣ envelope_h() + envelope_v()\nДля каждого пикселя смотрим окно 11×11\nНаходим самый тёмный (low) и светлый (high)\nсосед — это «огибающая» контраста"]

    D5["5️⃣ morph_close_x5()\nМорфологическое закрытие X-формой\nЗаполняем дырки в папиллярных линиях\n(5 проходов для надёжности)"]

    D6["6️⃣ local_stretch()\nНормализация: out = 255 − (px − low) × 255 / (high − low)\nКаждый пиксель растягивается на полный диапазон 0..255\nИнвертируем полярность (для NBIS нужно так)"]

    D7["✅ Готовое изображение\nuint8[5120] — 64×80 пикселей\nГотово для распознавания"]

    RAW --> D1 --> D2 --> D3 --> D4 --> D5 --> D6 --> D7

    style RAW fill:#f8f9fa,stroke:#aaa
    style D1 fill:#edf2ff,stroke:#5c7cfa
    style D2 fill:#edf2ff,stroke:#5c7cfa
    style D3 fill:#edf2ff,stroke:#5c7cfa
    style D4 fill:#edf2ff,stroke:#5c7cfa
    style D5 fill:#edf2ff,stroke:#5c7cfa
    style D6 fill:#edf2ff,stroke:#5c7cfa
    style D7 fill:#d3f9d8,stroke:#2f9e44
```

> 💡 **Зачем столько шагов?** Сенсор ёмкостный, не оптический. Он снимает не фото, а электрическое поле. Сигнал слабый и зашумлённый — поэтому нужна вся эта цепочка фильтров чтобы получить чёткие линии отпечатка.

---

## 🎯 Шаг 5: Контроль качества и повторы

```mermaid
flowchart TD
    IMG["🖼️ Обработанное изображение"]

    SCORE["📏 Считаем качество\nСтандартное отклонение\nцентральной зоны 48×64\n«Насколько контрастны линии?»"]

    CHECK{"Качество ≥ 25.0?\n(нормальный палец ~45–70\nпустой кадр ~5–10)"}

    GOOD["✅ Хороший кадр!\nОтправляем в NBIS"]

    BAD{"Попыток < 6\nИ бюджет < 600мс?"}

    RETRY["⏱️ Ждём 200мс\nПросим новый кадр"]

    BEST["😐 Берём лучший из всех\n(даже если не идеал)"]

    NBIS["🔍 NBIS — распознаватель\nИщет минуции (развилки линий)\nСравнивает с сохранёнными"]

    IMG --> SCORE --> CHECK
    CHECK -->|"да"| GOOD --> NBIS
    CHECK -->|"нет"| BAD
    BAD -->|"да"| RETRY --> SCORE
    BAD -->|"нет"| BEST --> NBIS

    style GOOD fill:#d3f9d8,stroke:#2f9e44
    style RETRY fill:#fff3bf,stroke:#e67700
    style BEST fill:#ffe3e3,stroke:#e03131
    style NBIS fill:#d3f9d8,stroke:#2f9e44
```

> 💡 **Зачем повторы?** Если ты приложил палец криво или влажный — первый кадр будет плохим. Система делает до 6 попыток за 600мс чтобы поймать хороший момент. Так улучшается точность на ~20%.

---

## 🔄 Шаг 6: Перезарядка сенсора

```mermaid
flowchart LR
    DONE["Кадр обработан"]

    R1["0x34 (два раза)\nСброс детектора пальца\n«Забудь что был касание»"]
    R2["⏳ Задержка 1500мс\nСенсор перезагружается"]
    R3["0xAE — Arm\nВключаем прерывания снова"]
    R4["0x32 — FDT\nЗапускаем детектор пальца"]
    R5["⏳ Ждём следующего пальца"]

    DONE --> R1 --> R2 --> R3 --> R4 --> R5

    style DONE fill:#d3f9d8,stroke:#2f9e44
    style R5 fill:#e7f5ff,stroke:#1971c2
```

> 💡 Без этого шага сенсор думает что палец всё ещё лежит и не отреагирует на следующее касание.

---

## 🔐 Финал: Распознавание

```mermaid
flowchart TD
    IMG["🖼️ Чистое изображение 64×80"]

    NBIS["🔬 NBIS (алгоритм NIST)\nИщет минуции — точки где:\n• линия раздваивается\n• линия заканчивается\nЭто уникальные маркеры отпечатка"]

    subgraph ENROLL["📝 Режим сохранения (enroll)"]
        E1["Собираем 5 кадров с разных углов"]
        E2["Строим шаблон (template)"]
        E3["Сохраняем в базу fprintd"]
        E1 --> E2 --> E3
    end

    subgraph VERIFY["✅ Режим проверки (verify)"]
        V1["Сравниваем с сохранёнными шаблонами"]
        V2{"Совпадение > порога?"}
        V3["✅ ДОСТУП РАЗРЕШЁН\nfprintd → PAM → Linux"]
        V4["❌ ОТКАЗ\nПопробуй снова"]
        V1 --> V2
        V2 -->|"да"| V3
        V2 -->|"нет"| V4
    end

    IMG --> NBIS
    NBIS --> ENROLL
    NBIS --> VERIFY

    style IMG fill:#edf2ff,stroke:#5c7cfa
    style NBIS fill:#d0ebff,stroke:#1971c2
    style V3 fill:#d3f9d8,stroke:#2f9e44
    style V4 fill:#ffe3e3,stroke:#e03131
```

---

## 🛡️ Защита от сбоев

```mermaid
flowchart TD
    subgraph HARD["🛡️ 10 защитных механизмов"]
        G1["G1: Сторожевой таймер\nINIT: 10 сек max\nCAP: 2 сек max\n→ если завис — убиваем"]

        G2["G2: Умный таймаут чтения\nТаймаут = мин(остаток_бюджета, 1с)\n→ не ждём вечно"]

        G3["G3: Умные USB ошибки\nОтличаем «устройство вынули»\nот «временный сбой»"]

        G4["G4: Режим восстановления\nЕсли REARM упал — делаем\nполный цикл SESSION→ARM→FDT\nбез TLS рестарта"]

        G8["G8: Отмена операций\nГлавный выключатель io_cancellable\nВсе USB операции отменяются мгновенно\nпри выключении драйвера"]

        G9["G9: Таймаут TLS\n5 секунд на рукопожатие\nSO_RCVTIMEO на сокете\n→ не зависаем навсегда"]

        G11["G11: Ресинхронизация\nПришёл мусор → убираем 1 байт\nи пробуем снова (не чистим весь буфер)\nПосле 2 таймаутов — новый триггер"]

        G13["G13: Таблица таймаутов\nВсе константы в одном файле\ngm168_timeouts.h\nС ассертами на компиляцию"]
    end

    style HARD fill:#fff5f5,stroke:#e03131
    style G1 fill:#ffe3e3,stroke:#e03131
    style G2 fill:#ffe3e3,stroke:#e03131
    style G3 fill:#ffe3e3,stroke:#e03131
    style G4 fill:#ffe3e3,stroke:#e03131
    style G8 fill:#ffe3e3,stroke:#e03131
    style G9 fill:#ffe3e3,stroke:#e03131
    style G11 fill:#ffe3e3,stroke:#e03131
    style G13 fill:#ffe3e3,stroke:#e03131
```

---

## 📁 Файлы проекта

```mermaid
flowchart LR
    subgraph FILES["📁 src/"]
        MAIN["goodix_gm168.c\n🧠 Главный файл\n1390 строк\nВсе 4 стейт-машины\n+ image pipeline"]

        PROTO["goodix_proto.c/h\n📦 USB кодек\nФормирует пакеты A0\nПарсит входящие"]

        TLS_F["goodix_tls.c/h\n🔐 TLS сервер\nmbedtls + pthread\n+ socketpair"]

        CAL["gm168_cal.h\n📐 Калибровочные таблицы\n(статические, опционально)"]

        TMO["gm168_timeouts.h\n⏱️ Все таймауты\nВ одном месте"]

        ERR["gm168_usb_errors.h\n❌ USB ошибки\nКлассификация"]

        TRC["gm168_trace.h\n🔍 Трассировка\nGM168_TRACE=1"]
    end

    MAIN --> PROTO
    MAIN --> TLS_F
    MAIN --> CAL
    MAIN --> TMO
    MAIN --> ERR
    MAIN --> TRC

    style MAIN fill:#ffe3e3,stroke:#e03131
    style PROTO fill:#e7f5ff,stroke:#1971c2
    style TLS_F fill:#f3d9fa,stroke:#9c36b5
    style CAL fill:#fff3bf,stroke:#e67700
    style TMO fill:#fff3bf,stroke:#e67700
```

---

## 🌊 Полный поток — одним взглядом

```mermaid
sequenceDiagram
    participant U as 👆 Ты
    participant S as 📡 Сенсор
    participant D as 🐧 Драйвер
    participant L as 🔐 Linux/PAM

    Note over D: Запуск драйвера
    D->>S: Wakeup + Reset
    D->>S: Version (кто ты?)
    S-->>D: v2.3.4 (например)
    D->>S: TLS Handshake (начало)
    S-->>D: TLS Handshake (ответ)
    Note over D,S: Зашифрованный канал установлен
    D->>S: Session + Arm + FDT
    D->>S: Захват ×5 (тёмные кадры)
    S-->>D: 5 пустых кадров
    Note over D: Считаем фон для вычитания

    Note over U,S: Ожидание

    U->>S: 👆 Прикладывает палец
    S-->>D: A0 status=0x02 (TOUCH!)

    D->>S: CAP_TRIG (снимай!)
    S-->>D: B2 пакеты (зашифровано)
    Note over D: TLS расшифровка → 10560 байт
    Note over D: decode → bg_subtract → blur → envelope → stretch
    Note over D: Качество = 62.4 ✅

    D->>L: Изображение 64×80
    Note over L: NBIS ищет минуции
    L-->>U: ✅ Доступ разрешён!

    Note over D: REARM: сброс + 1500мс + re-arm
```

---

## 📊 Цифры для понимания масштаба

| Что | Значение | Зачем |
|-----|----------|-------|
| Размер сенсора | 64 × 80 пикселей | Маленький, но достаточный |
| Размер кадра по USB | 10 560 байт | 80 строк × 132 байта |
| Глубина пикселя | 12 бит (0–4095) | Высокая точность измерения |
| Тёмных кадров для калибровки | 5 штук | Усредняем для точности |
| Порог качества | 25.0 (из ~100) | Плохие кадры отклоняем |
| Макс. попыток на касание | 6 | Если кривой палец — пробуем ещё |
| Бюджет времени | 600 мс | После этого берём лучшее из имеющегося |
| Размер ключа PSK | 32 байта = 256 бит | Военный уровень шифрования |
| Таймаут TLS рукопожатия | 5 секунд | После — считаем сенсор мёртвым |
| Таймаут INIT | 10 секунд | Вся инициализация должна уложиться |
